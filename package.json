{
  "name": "osu-collector-node",
  "version": "1.0.3",
  "description": "An osu!Collector api wrapper",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "repository": "https://github.com/roogue/osu-collector-node.git",
  "scripts": {
    "build": "tsc -p .",
    "start": "yarn build && node .",
    "test": "yarn build && node ./dist/_test/test.js",
    "pack": "yarn build && typedoc"
  },
  "author": "roogue",
  "license": "Apache-2.0",
  "dependencies": {
    "axios": "^0.27.2"
  },
  "devDependencies": {
    "typedoc": "^0.22.15"
  }
}
